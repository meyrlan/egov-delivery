import { Container } from "@mantine/core";

import { OperatorAllRequests } from "../molecules";

export const OperatorSystem = () => {
  return (
    <Container size={1600}>
      <OperatorAllRequests />
    </Container>
  );
};
